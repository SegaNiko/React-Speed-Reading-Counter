import WORDS_COUNT from "../consts/wordsCount";

const initialState = {
  wordsPage: WORDS_COUNT.DEFAULT,
  bookPages: null,
  readingSpeed: null,
  hoursToReadBook: null,
  modal: false,
};

export default initialState;
